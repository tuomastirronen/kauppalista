<h1>Kauppalista</h1>
<p>Lisää tuotteita ostoskoriin</p>

<%= render 'form' %>
<div class="row">
    <div class="col-md-12">
        <ul class="list-group">
            <% @items.each do |item| %>
                <%= render 'item', item: item %>
            <% end %>
        </ul>
    </div>
</div>